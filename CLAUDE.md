# مشروع: كشف أخطاء الإدخال والقيم الشاذة في ERPNext

## عن الشركة
شركة **الأقصى للتجارة الإلكترونية**: مجموعة وكلاء، وكل وكيل يسجّل طلباته بنفسه في ERPNext.
لذلك نحتاج مراقبة لحظية للطلبات وكشف أخطاء الإدخال قبل أن تصبح خسائر.

## سلسلة المستندات
```
Sales Order (طلب البيع)
   └── Purchase Invoice (فاتورة الشراء)   ← لكل طلب بيع فاتورة شراء
          └── فاتورة الشحن                ← تُضاف عند وصول الطلبية
```
الرابط الدقيق بين هذه المستندات غير معروف بعد (قد يكون في حقول مخصصة `custom_*`)، وأول مهمة هي اكتشافه.

## قواعد صارمة
1. **القراءة فقط.** المستخدم المستخدم في الـ API ليس له صلاحية كتابة. لا تحاول POST/PUT/DELETE أبداً.
2. **لا تطبع المفاتيح ولا تكتبها في أي ملف.** كلها في `.env` (وهو خارج Git).
3. أجب بالعربية، وبإيجاز ووضوح، مع أرقام وأسماء مستندات حقيقية (مثل `SAL-ORD-2026-00012`).
4. لا تغيّر بيانات ERPNext. اقتراحات الـ Server Scripts تُكتب في ملفات داخل `scripts/` لأراجعها أنا وأطبقها يدوياً.

## الأدوات
```bash
python erpnext_explorer.py map                      # اكتشاف الروابط بين المستندات
python erpnext_explorer.py schema "Sales Order"     # كل حقول المستند (القياسية والمخصصة والجداول الفرعية)
python erpnext_explorer.py sample "Sales Order"     # مستند كامل حديث كمثال
python erpnext_explorer.py fetch "Sales Order" --full   # كل السجلات من أول يوم
python erpnext_explorer.py export --full                # سحب كل المستندات الأساسية إلى data/
# للتقييد بفترة فقط (اختياري): أضف --days 30
python erpnext_explorer.py unpaid --top 30                  # أقدم الطلبات/الفواتير التي لم يكتمل دفعها
python erpnext_explorer.py debtors --top 20                 # أكثر العملاء ديوناً (بالمبلغ)
python erpnext_explorer.py debtors --by count               # أكثر العملاء عدداً للطلبات غير المسددة
# --source order (الافتراضي: إجمالي طلب البيع ناقص الدفعة المقدمة advance_paid) أو --source invoice (المتبقي في Sales Invoice)
```

## المهام بالترتيب
1. **الاكتشاف:** شغّل `map` و`schema` وحدد بدقة: كيف يرتبط Sales Order بـ Purchase Invoice وبفاتورة الشحن؟ وما اسم DocType فاتورة الشحن (قد تكون Purchase Invoice ثانية أو DocType مخصص)؟ اعرض لي النتيجة قبل المتابعة.
2. **السحب:** اسحب **كل السجلات من أول يوم** (بدون `--days`) بكل الحقول إلى `data/`. الكمية كبيرة، فشغّل السحب مع `--full` وأخبرني بالتقدم، ولا تعِد السحب إن كانت ملفات `data/` موجودة إلا إذا طلبت.
3. **فحص التطابق بين المستندات** (الأهم):
   - طلب بيع بدون فاتورة شراء بعد X أيام
   - إجمالي فاتورة الشراء لا يطابق الطلب أو يتجاوزه بنسبة معينة
   - وصلت الطلبية ولا توجد فاتورة شحن
   - تكلفة الشحن أعلى من نسبة معقولة من قيمة الطلب
   - تسلسل تواريخ غير منطقي (شحن قبل الشراء)
   - اختلاف العملة أو سعر الصرف
4. **فحص الإدخال:** كمية أو سعر صفر/سالب، سعر بيع أقل من الشراء، طلبات مكررة (نفس العميل + الصنف + اليوم)، حقول ناقصة.
5. **القيم الشاذة:** قارن السعر والكمية بتاريخ نفس الصنف ونفس الوكيل بطريقة **الوسيط ± 3×MAD** (لا المتوسط)، مع علامة وسبب لكل حالة وبدون حظر تلقائي.
6. **التقرير:** جدول بالحالات المشبوهة (رقم المستند، الوكيل، السبب، درجة الخطورة) + ملخص لكل وكيل: نسبة طلباته المُعلَّمة.
7. **التطبيق:** حوّل الفحوص المؤكدة إلى Server Scripts جاهزة للنسخ في ERPNext داخل `scripts/`.

## ملاحظات
- ERPNext على Frappe Cloud/CloudClusters، والإصدار غير محدد بعد (v14 أو v15).
- عند أي خطأ 403 على DocType معين، أخبرني باسمه لأضيف له صلاحية Read للـ Role `Api.only`.
